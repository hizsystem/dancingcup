# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# 댄싱컵 브랜딩 전략

HIZ가 댄싱컵(저가 커피 프랜차이즈, 매장 약 130개·경남권 중심)에 제안하는 브랜딩·SNS 운영 전략 작업공간이다.
산출물은 코드가 아니라 **HTML 슬라이드 덱**이며, 미팅 녹취·STT 원문을 근거 자료로 보관한다.

상위 워크스페이스 컨벤션은 `G:\-업무\히즈\.claude\CLAUDE.md` 참조 (인코딩, 바이너리 처리, 톤 가이드 등 공통 규칙).

---

## 폴더 구조

```
6. 댄싱컵/
├── CLAUDE.md                                    ← 이 파일
├── .gitignore                                   ← *.m4a, *.txt 제외
├── index.html                                   ← 최신 킥오프 덱 (= 260415 사본)
├── 댄싱컵_브랜딩전략_킥오프_260415.html         ← 날짜별 스냅샷
├── 음성 260406_160649.m4a                       ← 킥오프 녹취 원본
├── .claude/
│   └── agents/                                  ← 댄싱컵 에이전트 8개
├── _PM/                                         ← 리딩 단일 소스
│   ├── 로드맵.md
│   ├── 결정로그.md
│   ├── 리스크.md
│   ├── todo.md
│   ├── 마스터시트_정리.md
│   └── 주간/{YYMMDD}_주간플랜.md
├── 02_네이밍/                                   ← 딥라떼 네이밍 등
├── 03_톤매뉴얼/                                 ← 브랜드 톤·인스타 진단
├── 04_캠페인/{YYMMDD}_{캠페인명}/              ← 캠페인별 자료
├── 댄싱컵_마스터시트_디벨롭_260418.xlsx        ← 운영 단일 소스 (최신)
└── 0. 미팅/
    ├── 260406_킥오프 미팅/
    │   ├── 음성 260406_160649_original.txt     ← STT 원본
    │   └── 음성_utf8.txt                        ← UTF-8 정규화본
    └── 260415_부산 미팅/
        ├── 음성 260406_160649_original.txt
        └── 음성 260415_165609.m4a
```

## 에이전트 구조 (2026-04-18 세팅)

PM 중심 설계. 리드는 편집장, 콘텐츠 에이전트는 실행 레이어.

| 에이전트 | 역할 | 모드/작업 |
|---|---|---|
| `dancingcup-lead` | 편집장·모드 디스패처 | 요청 → 하위 에이전트 라우팅 + 리드 게이트 검수 + 미팅 기록 |
| `dancingcup-pm` | 프로젝트 매니지먼트 | 로드맵·주간플랜·TODO·리스크·결정로그·대시보드 유지 |
| `dancingcup-client` | 클라이언트 커뮤니케이션 | 미팅 아젠다·후속 메일·주간 보고·의사결정 요청서·커버레터 |
| `dancingcup-strategist` | 전략·덱 편집 | `index.html` 슬라이드 추가·수정·스냅샷 |
| `dancingcup-insta` | 인스타 공식 콘텐츠 | 감도 축 — 캘린더·피드·감도형 릴스·지점 확산 가이드 |
| `dancingcup-shortform` | 숏폼(릴스·쇼츠) | 바이럴 축 — 훅·스크립트·D-7~D+14 타임라인·재활용 버전 |
| `dancingcup-x` | X(트위터) | 대화 축 — 트윗·스레드·클레임 대응·운영자 캐릭터 |
| `dancingcup-viral` | 리서치 | 유행 훅·경쟁사 모니터링·소셜 리스닝·벤치마크 (증거 수집가) |

위임 원칙: PM·lead·client는 기획/검수만. 실제 콘텐츠 제작은 insta·shortform·x가 수행. 리서치는 viral이 공급.

## 단일 소스 2개

### 운영 데이터 SSoT — `댄싱컵_마스터시트_디벨롭_{YYMMDD}.xlsx`

18개 탭 (Summary · 비용 상세·마진 · 2026 예상 매출·지출 · **담당 업무 · 스케줄링 · 체크포인트** · 채널별 콘텐츠 캘린더 · 신메뉴 캠페인 · KPI 트래킹 · 키워드 데이터 · 인플루언서 리스트업·확정 · 파워 시딩·제보 · 블로그 체험단 관리 · 소셜 모니터링 · 지점 계정 디렉토리).

- **스케줄링 계열 3탭** (2026-04-21 개편) — 담당 업무(Who) / 스케줄링(When) / 체크포인트(TODO)
- **스코프** — P1 리테이너 운영 / P2 인플루언서 캠페인 2카테고리. 오프라인 업무는 HIZ 스코프 제외.
- **라떼 앵커(딥라떼 등)** = P1 리테이너 안 콘텐츠 피드. 단독 캠페인 아님. 네이밍은 HIZ 아이디어 제공 · 클라 내부 결정.

- **계약:** 2026.05~12 (8개월, 5월 시작) · 리테이너 500만/월 · 여름 캠페인(유행 메뉴 바이럴) 200만 · PPL 3,000~5,000만 *(4월은 사전 세팅/킥오프)*
- **클라:** (주)위드런 · 조기원 대표 · 실무 창구 = **변미혜 팀장** (withrun_ad@naver.com)
- **여름 D-Day:** 2026-05-12 (오미자에이드·컵빙수 3종·컵케이크)
- **인스타:** @dancingcup.official · 팔로워 2,518 · 게시물 487 (베이스라인)

상세·기록 루틴은 `_PM/마스터시트_정리.md` 참조.

### 리딩 SSoT — `_PM/`

- `로드맵.md` — 8개월(5~12월) 북극성·마일스톤 M0~M9·KPI 후보·의사결정 게이트
- `결정로그.md` — 확정·미확정·Pending 큐. **에이전트는 `확정`만 실행 기준으로 삼음**
- `리스크.md` — 가능성·영향·트리거·대응·담당·상태 6필드
- `todo.md` — 5-버킷 (사전/이번주/다음/나중/보류/완료)
- `마스터시트_정리.md` — 마스터시트 상태·업데이트 루틴·에이전트 연동
- `주간/{월요일 YYMMDD}_주간플랜.md` — 요일별 체크리스트 + 주간 랩업

### 충돌 우선순위

같은 정보가 두 곳에 있으면 **마스터시트가 우선**. `_PM/`은 요약·의사결정·주간 플래닝 관점에서만 유지.

## 핵심 원칙

- **결정 로그에 없는 것은 확정된 전략이 아니다.**
- **리스크는 실패 목록이 아니라, 미리 붙여둔 안전핀이다.**
- **TODO는 기억 보관함이 아니라, 이번 주 산출물로 끌어올릴 후보 큐다.**
- **2-Track = 딥라떼(B) × 트렌드 캐치(A)** — 대표가 직접 잡은 "두 바퀴" 방향 (마스터시트 Summary 메모 4번).

---

## 산출물 — `index.html`

단일 HTML 파일로 12개 슬라이드 덱을 구성한다. 외부 의존성 없음 (Pretendard 폴백 + 인라인 CSS).

**배포 (GitHub Pages):**

| URL | 용도 |
|-----|------|
| https://hizsystem.github.io/dancingcup/ | 댄싱컵 통합 허브 (index.html · 킥오프 덱) |
| https://hizsystem.github.io/dancingcup/quote.html | 월 운영 견적서 |
| https://hizsystem.github.io/dancingcup/ops.html | 채널 운영안 보고서 |

origin = `hizsystem/dancingcup` (2026-04-22 `dancingcup-branding-kickoff`에서 리네임).
`main` 브랜치에 푸시하면 GitHub Pages가 자동 서빙한다. 클라이언트 공유 링크는 위 URL 사용.

**덱 구조 (현재 v260415):**

| # | 섹션 | 내용 |
|---|------|------|
| 00 | Cover | 킥오프 표지 |
| 01 | 진단 | 댄싱컵 현황·시장 맥락·구조적 이점 |
| 02 | 소셜 리스닝 | voice cloud + 채널별 언급 강도 |
| 03 | 전략 프레임 | **2-Track**: 유행 훅(A) × 라떼 앵커(B) |
| 04 | 채널 역할 분리 | 인스타 공식(감도) vs 틱톡·X·지점(바이럴) |
| 05 | 인스타 운영 | 공식 / 지점 130개 / 인플루언서 3-레이어 |
| 06 | 틱톡 | 단일 공식 계정 운영 원칙 |
| 07 | X | 공식 어투 0% · 30분 룰 · 셀프디스 |
| 08 | Track A 런칭 루틴 | 신메뉴 D-7 ~ D+14 표준 타임라인 |
| 09 | Track B 라떼 앵커 | 감도 축적 × 상시 바이럴 |
| 10 | 벤치마크 | 텐퍼센트커피 "꾸덕라떼" 사례 |
| 11 | 톤 원칙 | 말하지 않을 것 (기업 어투·디스·성과 자랑) |
| 12 | Side Ideas | 메인 안착 후 검토할 보조 아이디어 |

**스타일 시스템 (slide head `<style>` 변수):**

- `--ink` `#1a1a1a` · `--paper` `#fafaf7` · `--cream` `#f4efe5` · `--accent` `#6b2c2c` (와인) · `--accent-soft` `#b8856a`
- 평가 톤: `--pos` 녹색 · `--neg` 와인 · `--neu` 중성 회색
- 슬라이드: 흰 카드 (`min-height: 580px`, `padding: 68px 76px`), 커버는 다크 잉크 배경
- 컴포넌트 클래스: `.slide`, `.kicker`, `.grid-2/3`, `.card[.soft|.dark]`, `.callout`, `.bridge`, `.tracks`, `.chan-split`, `.voice-cloud`, `.quote-box`, `.channel-row`
- A4 인쇄 미디어 쿼리 포함 (`@media print`)

---

## 작업 방식

### 덱 수정 시
- 슬라이드 번호는 `<div class="slide-num">NN</div>`로 표기. 추가/삭제 시 후속 번호 일괄 업데이트.
- 새 슬라이드는 `<!-- NN. 제목 -->` 주석 + `<section class="slide">` 패턴 유지.
- 기존 컴포넌트 클래스 재사용 우선. 새 클래스 추가는 head `<style>`에 정의.
- 색상 토큰은 `var(--name)`로만 사용 (하드코딩한 hex 직접 삽입 금지).

### 버전 관리
- `index.html`은 항상 최신본. 회차별 스냅샷은 `댄싱컵_브랜딩전략_{주제}_{YYMMDD}.html`로 별도 저장.
- 두 파일이 같은 시점이면 동일 내용. 작업 시작 시 diff로 확인.

### 미팅 녹취 활용
- STT 원본은 `0. 미팅/{YYMMDD}_{미팅명}/음성_*.txt`에 보관 (gitignore 대상).
- 한글 깨짐 시 `음성_utf8.txt` 사용. 없으면 직접 `encoding='utf-8'` 변환 후 저장.
- 발화자는 `발화자 1/2/3` 익명 표기. 1번이 HIZ 발표자 패턴.
- 새 미팅 추가 시 `0. 미팅/{YYMMDD}_{미팅명}/` 신규 폴더 생성.

### 콘텐츠 톤 (덱 수정 시 반드시 유지)
- **2-Track을 하나의 슬로건으로 묶지 않는다** (덱 03번 핵심 원칙).
- 인스타 공식 = 감도 / 숏폼·X·지점·인플루언서 = 바이럴, 톤 섞지 않음.
- 기업 어투·경쟁사 직접 디스·성과 자랑 금지 (덱 11번).
- 사실 근거: 덱 02번의 voice cloud / 덱 10번의 텐퍼센트 케이스는 실제 소비자 보이스·공개 자료 기반. 새 데이터 추가 시 출처 표기 (`<span class="src">`).

### 채널 축 (2026-04 협의 중)
- 신규 제작 중심: **인스타 / 숏폼(릴스·쇼츠) / X** 3축
- 틱톡: **보류 라벨**로 유지 — 경량 재포스팅 검토 수준. 결정 로그 확정 전 "제외" 단정 금지.
- 채널 확정은 M1 게이트 (2026-04-30).

### 인코딩 / 바이너리
- Windows + bash 환경. Python 실행 시 `sys.stdout.reconfigure(encoding='utf-8')` 필수.
- `.m4a` 직접 처리 불가 — 외부 STT 결과(`.txt`)만 활용.
- `.html`은 일반 텍스트로 Edit/Read 가능. 큰 파일이므로 부분 수정 시 정확한 슬라이드 단위로 범위 지정.
